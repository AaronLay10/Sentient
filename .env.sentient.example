# Sentient Engine Environment Configuration
# Copy this file to .env.sentient and fill in the values
# This file is used for BOTH development and production to ensure consistency

# ===================
# Database (PostgreSQL)
# ===================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=sentient
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
DATABASE_URL=postgres://postgres:your_secure_password_here@postgres:5432/sentient

# ===================
# Cache & Messaging (Redis)
# ===================
REDIS_URL=redis://redis:6379

# ===================
# MQTT Broker
# ===================
MQTT_USERNAME=sentient
MQTT_PASSWORD=your_mqtt_password_here

# ===================
# Authentication
# ===================
# Generate with: openssl rand -hex 32
JWT_SECRET=generate_a_long_random_string_here
JWT_EXPIRES_IN=1d

# Internal service-to-service token (for controller registration)
# Generate with: openssl rand -hex 32
INTERNAL_REG_TOKEN=generate_another_token_here

# ===================
# URLs (used by UI builds)
# ===================
# For local development
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_WS_URL=ws://localhost:3002

# For production (Dockerfile overrides these during build)
# VITE_API_URL=https://sentientengine.ai/api
# VITE_WS_URL=wss://sentientengine.ai/ws
