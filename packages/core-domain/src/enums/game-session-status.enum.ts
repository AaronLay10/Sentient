export enum GameSessionStatus {
  CREATED = 'created',
  READY = 'ready',
  RUNNING = 'running',
  PAUSED = 'paused',
  COMPLETED = 'completed',
  ABANDONED = 'abandoned',
  EMERGENCY_STOPPED = 'emergency_stopped',
}
